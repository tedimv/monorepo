import type { Component } from 'solid-js';

const About: Component = () => {
  return <div class="p-4 bg-slate-300 rounded-sm">This boilerplate is used to save time on setup every time</div>;
};

export default About;
